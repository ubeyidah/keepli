import { Button } from "@/components/ui/button";
import React from "react";
const page = async () => {
  return (
    <div>
      <h1>Keepli</h1>
      <Button>Get Started</Button>
    </div>
  );
};

export default page;
